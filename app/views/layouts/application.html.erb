<!DOCTYPE html>
<html>
  <head>
    <title>Cake2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <% if customer_signed_in? %>
        <li>
          <%= link_to "ログアウト(顧客)", destroy_customer_session_path,method: :delete %>
        </li>
        <li>
          <%= link_to "マイページ(顧客)", customer_path(current_customer.id) %>
        </li>
      <% else %>
        <li>
          <%= link_to "新規登録(顧客)", new_customer_registration_path %>
        </li>
        <li>
          <%= link_to "ログイン(顧客)", new_customer_session_path %>
        </li>
      <% end %>

      <% if admin_signed_in? %>
        <li>
          <%= link_to "ログアウト(管理者)", destroy_admin_session_path, method: :delete %>
        </li>
      <% else %>
        <li>
          <%= link_to "ログイン(管理者)", new_admin_session_path %>
        </li>
      <% end %>
    </header>
    <%= yield %>
  </body>
</html>
